web: php artisan serve --host 0.0.0.0 --port $PORT


**Action:** Before proceeding, please add this new `Procfile` to your project, commit it, and push it to your GitHub repository.

```bash
git add Procfile
git commit -m "Add Procfile for Railway deployment"
git push origin main